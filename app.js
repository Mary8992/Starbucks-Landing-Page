function imgSlider(product) {
    document.querySelector('.starbucks').src = product;
}

function changeCircleColor(color) {
    const circle = document.querySelector('.circle');
    circle.style.background = color;
}